<h2>Ponencias registradas</h2>
<% if @ponencias.nil? or @ponencias.empty? %>
  <p> Todavía no hay ponencias. ¡Empieza creando una! </p>
<% else %>
<div id="primarycontent">

  <% @ponencias.each do |p| %>
    <div class="post">
      <div class="header">
        <h3><%= p.titulo %></h3>
      </div>
      <div class="content">
        <p><%= p.descripcion %></p>
      </div>
      <div class="footer">
        <ul>
          <li>
            <%= link_to "Detalles", ponencia_path(p) %>    
          </li>
        </ul>
      </div>

  </div>
  <% end %>
</div>
<% end %>
      
